name: CI - Flask + Docker

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_DB: mydb
          POSTGRES_USER: user
          POSTGRES_PASSWORD: password
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          pip install -r app/requirements.txt

      - name: Lint Python code
        run: |
          pip install flake8
          flake8 app --max-line-length=120

      - name: Docker Compose Up and Test Endpoint
        run: |
          docker-compose up --build -d
          echo "⏳ Waiting for Flask app to be ready..."
          sleep 15

          echo "🌐 Curling the Flask endpoint..."
          curl -v --retry 5 --retry-delay 5 http://localhost:5000

          echo "🧪 Checking response contains PostgreSQL"
          curl -s http://localhost:5000 | grep "PostgreSQL"
